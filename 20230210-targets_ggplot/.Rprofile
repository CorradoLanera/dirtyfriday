source("renv/activate.R")

if (interactive()) {
  suppressWarnings(suppressPackageStartupMessages({
    library("devtools")
    library("usethis")
    library("testthat")
    library("checkmate")
  }))
}
